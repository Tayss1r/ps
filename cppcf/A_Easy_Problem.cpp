#include <bits/stdc++.h>
#define all(t) t.begin(),t.end()
#define for(t) for (int i = 0; i < t; i++)
using namespace std;

void solve() {
    int n;
    cin >> n;
    cout << n-1 << endl;
}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}